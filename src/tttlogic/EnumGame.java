package tttlogic;

public enum EnumGame {
    ;
    public enum State {
        WIN, DRAW, NOTHING, SET
    }

    public enum Type {
        EASY, MIDDLE, HARD, USER
    }

    public enum Online {
        SERVER, CLIENT
    }
}
